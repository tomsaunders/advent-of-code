#!/usr/bin/env npx ts-node
import * as fs from "fs";
import { test } from "./util";

const input = 36000000;
function lowestHouse(goal: number): number {
  return 0;
}
test(1, lowestHouse(10));
test(9, lowestHouse(130));
